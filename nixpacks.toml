# Coolify/Nixpacks: install devDependencies for Next.js build (TypeScript, Tailwind, Prisma CLI)
[phases.install]
cmds = ["pnpm install --frozen-lockfile --prod=false"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm start"
